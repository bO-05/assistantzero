# Mistral API Key for AI SDK
MISTRAL_API_KEY="YOUR_MISTRAL_API_KEY"
# Optional: Specify Mistral models to use (defaults shown below)
# MISTRAL_CHAT_MODEL="mistral-small-latest"
# MISTRAL_EMBEDDING_MODEL="mistral-embed"
LANGCHAIN_CALLBACKS_BACKGROUND=false

# Exa AI - Semantic web search (better than traditional search!)
# Get your key at: https://exa.ai (free tier available)
EXA_API_KEY="YOUR_EXA_API_KEY"

# Auth0 configuration
# For local development: http://localhost:3000
# For production: https://assistant0agent.vercel.app
APP_BASE_URL="http://localhost:3000"
AUTH0_SECRET="use [openssl rand -hex 32] to generate a 32 bytes value"
AUTH0_DOMAIN="{yourDomain}.us.auth0.com"
AUTH0_CLIENT_ID="{yourClientId}"
AUTH0_CLIENT_SECRET="{yourClientSecret}"

# Optional: Auth0 API Configuration (for async authorization/CIBA)
# AUTH0_CUSTOM_API_CLIENT_ID="{yourApiClientId}"
# AUTH0_CUSTOM_API_CLIENT_SECRET="{yourApiClientSecret}"
# AUTH0_AUDIENCE="https://your-app-url.com/api"
# AUTH0_SCOPE="openid profile email offline_access"

# Database configuration
# For local development: Use local PostgreSQL
# For production: Recommended to use Neon (https://neon.tech) with pgvector enabled
# Example Neon URL: postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_documents_db"   

# Auth0 FGA (Fine-Grained Authorization)
# Get these from: https://dashboard.fga.dev
FGA_STORE_ID="{your-fga-store-id}"
FGA_CLIENT_ID="{your-fga-store-client-id}"
FGA_CLIENT_SECRET="{your-fga-store-client-secret}"
FGA_API_URL="https://api.us1.fga.dev"
FGA_API_AUDIENCE="https://api.us1.fga.dev/"
FGA_API_TOKEN_ISSUER="auth.fga.dev"
# FGA_MODEL_ID will be created when you run: npm run fga:init
# FGA_MODEL_ID="{generated-after-fga-init}"

# Optional: Other model keys
# ANTHROPIC_API_KEY="YOUR_API_KEY"

# Turn on demo mode (RECOMMENDED FOR HACKATHON JUDGES)
# When enabled, Gmail/Calendar tools show realistic demo data WITHOUT requiring Google OAuth
# This solves the OAuth Consent Screen test user limitation
# Set to "true" for hackathon presentations where judges can't be whitelisted
NEXT_PUBLIC_DEMO="true"

# Shop API URL
SHOP_API_URL="http://localhost:3001/api/shop"
SHOP_API_AUDIENCE="https://api.shop-online-demo.com"
